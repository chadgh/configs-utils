[user]
	name = chadgh
	email = chadgh@gmail.com
[pager]
	diff = 
[color]
	diff = auto
	status = auto
[color "diff"]
	meta = yellow bold
	frag = magenta bold
	old = red bold
	new = green bold
[color "status"]
	added = yellow
	changed = green
	untracked = cyan
[alias]
	co = checkout
	s = status
	ci = commit
	b = branch
	switch = checkout
	lol = log --abbrev-commit --graph --decorate --all
	lolo = log --pretty=oneline --abbrev-commit --graph --decorate --all
[push]
	default = matching
